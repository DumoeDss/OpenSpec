## 1. Lock the repo-local scope contract with failing tests

- [x] 1.1 Extend `test/core/root-selection.test.ts` with nearest-discovery cases for an unbound project with a qualifying local `rasen/` tree and no selector: `planningScope.kind` is `standalone`, `planningScope.paths` names `active-changes`, `archive-line`, `specs`, `project-home`, and `project-design-docs` inside that tree (expected values built with `path.join`), `root.source` stays `nearest`, and `storeId` is absent.
- [x] 1.2 Add the inheritance and identity variants: a `store:` configuration-inheritance declaration and a `projectId` in `rasen/config.yaml` keep `root.source === 'nearest'`, still report the `inheriting-store-config` notice exactly once, and expose `planningScope.source === 'project-binding'` with `kind: standalone`.
- [x] 1.3 Add the non-adoption cases: an unregistered legacy flat Store checkout (Store metadata present, no registry match) and a root with an unreadable Store metadata file both return today's scope-less compatibility root, so `storeFinalizationDiagnostic` in `src/core/archive.ts` still refuses the legacy flat tree by classification; a nearest Store v2 project and a `--project` standalone selection keep their current results.
- [x] 1.4 Add `test/commands/repo-local-typed-scope.test.ts`: an isolated temporary fixture with its own `HOME`/`USERPROFILE`/`XDG_CONFIG_HOME`/`XDG_DATA_HOME`, `RASEN_LANG=en`, no inherited `RASEN_*` variables, and separate `git init` repositories for the product checkout and its `rasen/` planning tree; drive the built CLI for `list --json`, `status --change <name> --json`, `instructions proposal --change <name> --json`, and `context --json` and assert identical `root.scope.paths` across commands, `changeRoot`/`evidenceDir`/`handoffDir`/`planningHome.changesDir` contained by `root.scope.paths["active-changes"]`, and `actionContext.planningWriteRoots` equal to the scope's `specs` and `active-changes`.
- [x] 1.5 Cover the no-active-Change variant of the same fixture (`list --json` and `context --json` with an empty `rasen/changes`) and confirm the red gate against the unmodified `src/core/root-selection.ts` before any production edit: every scope-positive case (1.1, 1.2, 1.4, 1.5) fails, while the preservation-guard cases in 1.3 (unregistered legacy flat Store checkout, unreadable Store metadata including an `ENOTDIR` metadata path, nearest Store v2 project, `--project` standalone) already pass and must keep passing after the repair.

## 2. Repair the nearest-root handoff

- [x] 2.1 In `resolveOpenSpecRoot` (`src/core/root-selection.ts`, nearest-root branch), when the planning result's `planningScope.kind` is `standalone`, await the existing `resolveStandaloneOrLegacyRoot(options)` call so its notices and diagnostics run once and unchanged, then adopt the already-resolved scoped root only when the `readOptionalStoreMetadataState(nearest)` read in that branch returned `null`, the compatibility answer has `source: 'nearest'` and no `storeId`, and the compatibility root path and the scoped root path are equal under the existing `samePathForPlatform` comparison (both are already canonical root values; add no `realpath` call).
- [x] 2.2 Return the scoped root with `source` taken from the compatibility answer; keep its resolver-owned capability, description, planning paths, and any resolver-provided `executionRoot` without inventing an execution locator.
- [x] 2.3 Leave every other branch untouched: `legacy-store` answers, planning-resolver failures, `declaresAmbientStoreFact`, `declaresLayoutV2`, the `--store`/`--project`/`--target-line` branches, `resolveOpenSpecRootThroughPlanning`, `describePaths`, `toRootOutput`, and `makeRoot`; introduce no new diagnostic, notice, option, or path synthesis.
- [x] 2.4 Remove the authoring-only scope assumption from the explanatory comment above `classifyStoreRootLayout` in `src/core/archive.ts`; retain the explanation of scope-less legacy Store classification and leave refusal logic unchanged.
- [x] 2.5 Update assertions elsewhere in `test/` that pinned the defect for repo-local output (absence of `root.scope`, or `actionContext.readRoots` equal to only the checkout root) to the scope-derived values; delete rather than re-pin any assertion that only encoded the missing scope.

## 3. Verify the bounded change

- [x] 3.1 Run the focused suites `test/core/root-selection.test.ts` and `test/commands/repo-local-typed-scope.test.ts` and confirm every case from group 1 passes.
- [x] 3.2 Run `node test/fixtures/repo-local-typed-scope-probe.mjs bin/rasen.js` and the same command with `--empty` against the rebuilt CLI and record `PASS` for `list`, `status`, and `context` (three lines) and for `list` and `context` (two lines) respectively; then delete `test/fixtures/repo-local-typed-scope-probe.mjs`, leaving the permanent regression tests from group 1 as the only coverage.
- [x] 3.3 Run TypeScript and lint checks for `src/core/root-selection.ts` and the touched test files, then run the root-selection, store root-selection, and archive test groups once after integration to confirm legacy flat Store refusals and Store v2 routing are unchanged.
- [ ] 3.4 Verify `test/commands/repo-local-typed-scope.test.ts` and the extended `test/core/root-selection.test.ts` on the Windows CI shards and a POSIX CI job, recording the run id and head SHA. On Windows, starting paths that differ by drive-letter case or separator form must resolve to the same standalone scope with native `root.scope.paths` identifying the same canonical locations under platform-aware comparison and no false binding conflict; this Change does not extend the resolver's path-spelling normalization, so no byte-identical casing is asserted and expected values are not rewritten as platform-specific strings. This task stays unchecked until those shards have actually run.
- [x] 3.5 Run `RASEN_LANG=en node bin/rasen.js validate fix-repo-local-typed-scope-discovery --type change --strict --json --no-interactive` and confirm zero issues; audit the diff against `src/core/root-selection.ts`, the comment in `src/core/archive.ts`, `CHANGELOG.md`, focused tests, and this Change's artifacts. Fixture commands must not modify data outside their temporary roots. Preserve existing user changes in `rasen/config.yaml`, all canonical specs, and every Ghostty file.
- [x] 3.6 Add a `### Changed` entry under `## Unreleased` in `CHANGELOG.md` stating that repo-local planning roots discovered as the nearest root now report their authoritative `root.scope` (kind, source, typed paths) in `list`, `status`, `instructions`, and `context` JSON while `root.path` and `root.source` are unchanged.

## 4. Prove the standalone lifecycle on an isolated fixture only

- [x] 4.1 Run `node test/fixtures/repo-local-typed-scope-smoke.mjs bin/rasen.js` against the rebuilt CLI. The fixture builds a temporary product checkout and nested `rasen/` planning tree as separate Git repositories with isolated `HOME`/`XDG` and no inherited `RASEN_*` or `GIT_*` variables, then proves in order: typed-path comparison (`root.scope.kind` `standalone`, identical `ref` across `list`, `status`, and `context`, `changeRoot` under `active-changes`), `validate --type change --strict`, `archive --intent-template`, the saved immutable preview (`--dry-run --save-plan`) with `plan.complete` and `validation: passed`, `archive --apply-plan` with exactly the returned `archive-v1:` token, a second apply of the same token that replays as `complete`, canonical spec synchronization inside the fixture, commit and push of the nested planning repository to a local bare remote, and unchanged evidence bytes, `archive.json`, and product `HEAD`. Record the single `PASS` line.
- [x] 4.2 Keep the lifecycle proof isolated: do not archive this Change, synchronize the real canonical spec, run ship, create a PR, or read/write Ghostty planning data. Any proposal or implementation commit/push is a separate repository-scoped action, not evidence of archive or merge.
- [x] 4.3 Confirm the fixture's `archive.json` and evidence bytes remain identical across completed replay and nested planning commit/push, with product HEAD unchanged. Record the observed verification results in this still-active Change's evidence; the post-hash immutability rule applies only after that Change is eventually archived by its owner.
